const {ccclass, property} = cc._decorator;

@ccclass
export default class PlayerView extends cc.Component {

    

    // onLoad () {}

    start () {
        cc.find("地主标志", this.node).active = false;
        cc.find("cards", this.node).active = false;
        cc.find
    }

    showBankerNode() {
        
    }

    

    // update (dt) {}
}
